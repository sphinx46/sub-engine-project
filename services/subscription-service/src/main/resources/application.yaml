spring:
  application:
    name: subscription-service
  profiles:
    active: dev

server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

logging:
  level:
    mordvinov_dev.subscription_service: DEBUG
    org.springframework.web: INFO
    org.springframework.security: DEBUG

springdoc:
  api-docs:
    path: /api-docs
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
  packages-to-scan: mordvinov_dev.subscription_service
