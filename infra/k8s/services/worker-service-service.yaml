apiVersion: v1
kind: Service
metadata:
  name: worker-service
  namespace: sub-engine
  labels:
    app: worker-service
spec:
  selector:
    app: worker-service
  ports:
    - port: 8083
      targetPort: 8083
      name: http
  type: ClusterIP