apiVersion: v1
kind: Service
metadata:
  name: eureka-server
  namespace: sub-engine
  labels:
    app: eureka-server
spec:
  selector:
    app: eureka-server
  ports:
    - port: 8761
      targetPort: 8761
      name: http
  type: ClusterIP