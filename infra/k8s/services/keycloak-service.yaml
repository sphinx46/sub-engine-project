apiVersion: v1
kind: Service
metadata:
  name: keycloak
  namespace: sub-engine
  labels:
    app: keycloak
spec:
  selector:
    app: keycloak
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  type: ClusterIP