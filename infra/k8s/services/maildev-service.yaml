apiVersion: v1
kind: Service
metadata:
  name: maildev
  namespace: sub-engine
  labels:
    app: maildev
spec:
  selector:
    app: maildev
  ports:
    - port: 1080
      targetPort: 1080
      name: web
    - port: 1025
      targetPort: 1025
      name: smtp
  type: ClusterIP