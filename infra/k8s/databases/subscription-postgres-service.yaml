apiVersion: v1
kind: Service
metadata:
  name: subscription-postgres
  namespace: sub-engine
  labels:
    app: subscription-postgres
spec:
  selector:
    app: subscription-postgres
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  clusterIP: None