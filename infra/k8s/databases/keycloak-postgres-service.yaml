apiVersion: v1
kind: Service
metadata:
  name: keycloak-postgres
  namespace: sub-engine
  labels:
    app: keycloak-postgres
spec:
  selector:
    app: keycloak-postgres
  ports:
    - port: 5432
      targetPort: 5432
      name: postgres
  clusterIP: None